<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="../../../static/css/style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quest Store</title>
</head>

<body>

<header>
    <div class="header">
        <div id="logo">
            <a href="/queststore/codecooler/{{codecooler.getId()}}"><img src="../../../static/img/logo6.png" alt="logo"></a>
        </div>

        <div id="main-nav">
            <a href="/queststore/codecooler/{{codecooler.getId()}}">My Profile</a>
            <a href="/queststore/codecooler/experience/{{codecooler.getId()}}">Experience</a>
            <a href="/queststore/codecooler/wallet/{{codecooler.getId()}}">Wallet</a>
            <a class="active" href="/queststore/codecooler/store/{{codecooler.getId()}}">Store</a>
            <a href="/queststore/codecooler/logout/{{codecooler.getId()}}">Logout</a>
        </div>
    </div>
</header>
<div class="displayStoreContent">
    <div id="contener">
        <div id="heading">
            <h1>Store</h1>
        </div>
        <div id="content" style="overflow-x:auto;">
            <form method = "POST" id = "storeform">
            <table id="storetable">
                <tr class="table-top">
                    <th class="table-title">Basic items</th>
                    <th class="table-title">Description</th>
                    <th class="table-title">Price</th>
                    <th class="table-title"></th>
                </tr>

                {% for  item in itemsBasic %}
                <tr>
                    <td>{{item.getName()}}</td>
                    <td>{{item.getDescription()}}</td>
                    <td>{{item.getPrice()}}&nbsp;cc</td>
                    <td><input type = "checkbox" name = "{{item.getId()}}"/></td>
                </tr>
                {% endfor %}

                <tr class="table-top">
                    <th class="table-title">Magic items</th>
                    <th class="table-title">Description</th>
                    <th class="table-title">Price</th>
                    <th class="table-title"></th>
                </tr>
                {% for  item in itemsMagic %}
                <tr>
                    <td>{{item.getName()}}</td>
                    <td>{{item.getDescription()}}</td>
                    <td>{{item.getPrice()}}&nbsp;cc</td>
                    <td><input type = "checkbox" name = "{{item.getId()}}"/></td>
                </tr>
                {% endfor %}

            </table>
                <input type = "submit" value = "Buy" />
            </form>
        </div>
    </div>

    <footer>
        <div id="footer-cont">
            <div>
                <p>© Quest Store 2019</p>
            </div>
            <div id="footer-logo">
                <img src="../../../static/img/whitering60.png">
            </div>
            <div>
                <p>Created by:</p>
                <ul id="autors">
                    <li>Ewelina Jaworska</li>
                    <li>Konrad Suseł</li>
                    <li>Kamil Olsza</li>
                    <li>Andrzej Rzeszut</li>
                </ul>
            </div>

        </div>

    </footer>
</div>
</body>
</html>

